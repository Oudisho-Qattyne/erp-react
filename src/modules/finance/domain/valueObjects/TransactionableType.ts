// The kind of entity a transaction is linked to.
// "general" = not linked to any specific entity.
// Future values (e.g. "fee", "contract") will each map to their own picker dialog.
export type TransactionableType = "general"
